//! NewBCPL typed IR.
//!
//! Stub. Phase-visible IR sits between sema and the LLVM emit stage; it is
//! the artifact dumped by `dump-ir`. CFG construction lives here too
//! (dumped by `dump-cfg`).
