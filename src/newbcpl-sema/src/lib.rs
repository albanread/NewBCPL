//! NewBCPL semantic analysis: name resolution, type inference, class layout.
//!
//! Stub. Sema will resolve identifiers, infer types (the dialect is largely
//! typeless but distinguishes integer/float by operator family — see
//! `reference/documentation/BCPL float extension.md`), build the class table
//! and lay out vtables per `reference/documentation/classes_and_objects.md`.
